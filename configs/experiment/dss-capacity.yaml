# @package _global_
defaults:
  - /pipeline: capacity
  - /model: dss

dataset:
  inp_length: 10000
  n_memorize: 8
  n_samples: 1000

model:
  dropout: 0.0
  n_layers: 1
  prenorm: true
  d_model: 64
  norm: layer

optimizer:
  lr: 0.001

loader:
  batch_size: 20

trainer:
  max_epochs: 5000

train:
  seed: 1112
